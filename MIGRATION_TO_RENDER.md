# 🚀 Міграція з Railway на Render

Цей гайд допоможе вам мігрувати ваш Dart backend з Railway на Render (безкоштовний план).

---

## 📋 Перед початком

### Що потрібно:
- ✅ GitHub репозиторій з вашим кодом
- ✅ Аккаунт на [Render.com](https://render.com)
- ✅ Доступ до поточного Railway проекту (для експорту даних)

### Що буде мігровано:
- ✅ Backend сервіс (Dart)
- ✅ PostgreSQL база даних
- ✅ Змінні середовища
- ✅ Автоматичний деплой з GitHub

---

## 🔄 Крок 1: Експорт даних з Railway

### 1.1 Експорт бази даних

```bash
# Встановіть Railway CLI (якщо ще не встановлено)
npm install -g @railway/cli

# Залогіньтесь
railway login

# Підключіться до проекту
railway link

# Отримайте connection string
railway variables

# Експортуйте базу даних
pg_dump $DATABASE_URL > backup.sql
```

Або через Railway Dashboard:
1. Перейдіть до вашого PostgreSQL сервісу
2. Відкрийте "Connect" → "Postgres URL"
3. Використайте будь-який PostgreSQL клієнт для експорту

---

## 🆕 Крок 2: Створення проекту на Render

### 2.1 Реєстрація

1. Перейдіть на [Render.com](https://render.com)
2. Натисніть "Get Started for Free"
3. Зареєструйтесь через GitHub (рекомендовано)

### 2.2 Створення через Blueprint (рекомендовано)

1. Натисніть "New +" → "Blueprint"
2. Підключіть ваш GitHub репозиторій
3. Render автоматично визначить `render.yaml`
4. Перевірте налаштування:
   - **Service Name:** `todo-backend`
   - **Database Name:** `todo-postgres`
5. Натисніть "Apply"

### 2.3 Альтернатива: Ручне створення

#### Створення PostgreSQL:

1. Натисніть "New +" → "PostgreSQL"
2. Налаштування:
   - **Name:** `todo-postgres`
   - **Database:** `todo_db` (або залиште за замовчуванням)
   - **User:** `todo_user` (або залиште за замовчуванням)
   - **Plan:** `Free`
3. Натисніть "Create Database"
4. Запишіть дані підключення (вони будуть потрібні)

#### Створення Web Service:

1. Натисніть "New +" → "Web Service"
2. Підключіть GitHub репозиторій
3. Налаштування:
   - **Name:** `todo-backend`
   - **Environment:** `Docker`
   - **Region:** Оберіть найближчий (наприклад, Frankfurt)
   - **Branch:** `main` (або ваша основна гілка)
   - **Root Directory:** `/` (залиште порожнім)
   - **Dockerfile Path:** `./Dockerfile`
   - **Docker Command:** `/app/bin/server`
   - **Plan:** `Free`
4. Натисніть "Advanced" і додайте змінні середовища:

```bash
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=production
ALLOWED_ORIGINS=*
LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=true
```

5. Підключіть базу даних:
   - В розділі "Environment" натисніть "Link Resource"
   - Оберіть `todo-postgres`
   - Render автоматично додасть змінні:
     - `DATABASE_URL` (Internal)
   - Додайте вручну з Internal Database URL:
     ```
     DB_HOST=<Internal Database Host>
     DB_PORT=<Internal Database Port>
     DB_NAME=<Internal Database Name>
     DB_USER=<Internal Database User>
     DB_PASSWORD=<Internal Database Password>
     ```

6. Натисніть "Create Web Service"

---

## 📥 Крок 3: Імпорт даних

### 3.1 Отримання connection string з Render

1. Перейдіть до вашого PostgreSQL сервісу на Render
2. Відкрийте "Info" → "Internal Database URL"
3. Скопіюйте connection string

### 3.2 Імпорт даних

```bash
# Якщо у вас є backup.sql з Railway
psql $RENDER_DATABASE_URL < backup.sql

# Або через pgAdmin / DBeaver
# Використайте Internal Database URL з Render
```

**Важливо:** Використовуйте **Internal Database URL** для імпорту, а не External URL.

---

## ✅ Крок 4: Перевірка

### 4.1 Перевірка деплою

1. Зачекайте завершення деплою (5-10 хвилин)
2. Перевірте логи в Render Dashboard
3. Перевірте health endpoint:

```bash
curl https://your-app.onrender.com/api/health
```

### 4.2 Перевірка підключення до БД

```bash
# Перевірте через API
curl https://your-app.onrender.com/api/todos/all
```

---

## 🔧 Крок 5: Оновлення фронтенду (якщо потрібно)

Оновіть URL API у вашому фронтенді:

```javascript
// Було (Railway)
const API_URL = 'https://your-app.railway.app';

// Стало (Render)
const API_URL = 'https://your-app.onrender.com';
```

---

## ⚠️ Відмінності між Railway та Render

### Render (безкоштовний план):
- ✅ Безкоштовний
- ⚠️ Спиння після 15 хв неактивності
- ⚠️ Перший запит після "засинання" може бути повільним (30-60 сек)
- ✅ Автоматичний SSL
- ✅ Автоматичний деплой з GitHub

### Railway:
- ❌ Платний після безкоштовного trial
- ✅ Завжди активний
- ✅ Швидший старт
- ✅ Автоматичний SSL
- ✅ Автоматичний деплой з GitHub

---

## 🐛 Вирішення проблем

### Проблема: Деплой не запускається

**Рішення:**
1. Перевірте логи в Render Dashboard
2. Перевірте що Dockerfile правильний
3. Перевірте що всі змінні середовища встановлені

### Проблема: Помилка підключення до БД

**Рішення:**
1. Переконайтесь що використовуєте **Internal Database URL**
2. Перевірте що база даних створена і активна
3. Перевірте змінні середовища `DB_*`

### Проблема: Сервіс "засинає"

**Рішення:**
- Це нормально для безкоштовного плану
- Перший запит після "засинання" може займати 30-60 секунд
- Для production розгляньте платний план ($7/міс)

### Проблема: Повільний деплой

**Рішення:**
- Перший деплой завжди довший (5-10 хвилин)
- Наступні деплої швидші (2-3 хвилини)
- Це нормально для безкоштовного плану

---

## 📚 Корисні посилання

- [Render Documentation](https://render.com/docs)
- [Render Pricing](https://render.com/pricing)
- [Render Community](https://community.render.com)

---

## ✅ Чеклист міграції

- [ ] Експортовано дані з Railway
- [ ] Створено аккаунт на Render
- [ ] Створено PostgreSQL базу даних на Render
- [ ] Створено Web Service на Render
- [ ] Налаштовано змінні середовища
- [ ] Підключено базу даних до сервісу
- [ ] Імпортовано дані в нову базу
- [ ] Перевірено health endpoint
- [ ] Перевірено роботу API
- [ ] Оновлено URL у фронтенді
- [ ] Видалено старий проект на Railway (опціонально)

---

**Готово! 🎉 Ваш backend тепер працює на Render безкоштовно!**







